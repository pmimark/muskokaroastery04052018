- content_for :title do
  Preview Image

%p
  = image_tag @image.image.url(:large)
%hr
%p
  = image_tag @image.image.url(:medium)
  = image_tag @image.image.url(:small)
  = image_tag @image.image.url(:tiny)
  = image_tag @image.image.url(:tiny_square)
  = image_tag @image.image.url(:thumb)
%hr
%p
  = image_tag @image.image.url(:square)
  = image_tag @image.image.url(:featured)

- content_for :sidebar do
  %p= link_to "Edit Image", edit_image_path(@image), :class => "feature-button"
  %br
  %p= link_to 'Back to List', images_path
