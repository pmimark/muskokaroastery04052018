- content_for :title do
  Listing Shipping Rates

%table.admin
  %tr
    %th Name
    %th Country Code
    %th Price
    %th Free Shipping
    %th

  - @shipping_rates.each do |shipping_rate|
    %tr{ :class => "#{ cycle("even", "odd") }" }
      %td= shipping_rate.name
      %td= shipping_rate.country_code
      %td= number_to_currency shipping_rate.price
      %td= number_to_currency shipping_rate.free_price
      %td= link_to 'Edit', edit_shipping_rate_path(shipping_rate)
