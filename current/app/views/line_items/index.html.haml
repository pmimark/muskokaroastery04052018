%h1 Listing line_items

%table
  %tr
    %th Product
    %th Quantity
    %th Order
    %th Item number
    %th Item price
    %th
    %th
    %th

  - @line_items.each do |line_item|
    %tr
      %td= line_item.product_id
      %td= line_item.quantity
      %td= line_item.order_id
      %td= line_item.item_number
      %td= line_item.item_price
      %td= link_to 'Show', line_item
      %td= link_to 'Edit', edit_line_item_path(line_item)
      %td= link_to 'Destroy', line_item, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Line item', new_line_item_path
